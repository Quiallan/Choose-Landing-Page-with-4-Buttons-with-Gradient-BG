/* Rabbit Hole Page CSS */
#rabbitHolePage {
    background-color: rgb(157, 11, 11);
}

/* Or using the class to set the background color to black */
.unique-body {
    background-color: black;
}


.rabbit-hole-page {
    background-color: black !important; /* Using !important to override any other styles */
}
kbody {
    background: linear-gradient(to bottom, black, blue);
  }
  

  body {
    margin: 0;
    padding: 0;
   /* background-image: url('dirts.jpg'); /* Added background image */*/
    background-size: cover; /* Cover the entire viewport */
    background-position: center;
    background-repeat:
    background-color: #000000; /*#2c1708;*/
    overflow-x: hidden;
  }
  
  * {
    box-sizing: border-box;
  }
  .boxed-layout {
    margin: 0 auto;
    max-width: 1200px;
    background-color: #ffffff; /* White background */
    overflow: hidden;
    box-shadow: 0 4px 8px rgba(0, 0, 0, 0.1);
  }
  

  
  .rabbit-hole-container {
    width: 100%;
    height: 100vh;
    position: relative;
    background-color: red;
    /* Remove the background image styling from here */
  }
  
  .rabbit-hole-container::before {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: url('rabbitholes.jpg');
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    opacity: 0.99; /* 50% opacity */
    z-index: 0;
  }
  
  .rabbit-hole-container::after {
    content: "";
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background-image: url('dirt-graphics.png');
    background-size: cover;
    background-position: center;
    background-repeat: no-repeat;
    z-index: 1;
  }
  
  .button-grid {
    display: grid;
    grid-template-columns: 1fr 1fr;
    grid-template-rows: 1fr 1fr;
    gap: 20px;
    justify-items: center;
    align-items: center;
    width: 100%;
    height: 100%;
    position: relative;
    z-index: 2;
  }
  
 
  
  .rabbit-button {
    /* Styles will be added later */
  }
  #matrixCanvas {
    position: absolute;
    top: 0;
    left: 0;
    z-index: -1; /* To keep it in the background */
  }
